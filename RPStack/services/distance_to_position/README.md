# DistanceToPosition

Converts a raw `sensing.distance_observer` capability into a linear
`motion.position_observer`. This is where installation-specific zero offset,
direction, reference frame, and usable range belong.
