# env tests

This directory is reserved for host-side tests for the env runtime component. Device and hardware integration checks belong here as isolated tests and must not run automatically when they can alter device state.
